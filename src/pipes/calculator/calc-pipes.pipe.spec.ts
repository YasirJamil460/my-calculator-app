import { CalcPipesPipe } from './calc-pipes.pipe';

describe('CalcPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new CalcPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
